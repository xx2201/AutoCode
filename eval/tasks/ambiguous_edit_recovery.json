{
  "id": "ambiguous_edit_recovery",
  "title": "Recover after an ambiguous edit attempt",
  "prompt": "Read service.py first. Start with a minimal `edit_file` attempt that replaces the exact shared line `return \"placeholder\"` with `return \"ready\"`. That first attempt should fail because the same line appears multiple times. Recover by making more specific edits until both functions return `ready`, then verify with `python service.py`.",
  "fixture": "ambiguous_edit_recovery",
  "tags": ["recovery", "editing"],
  "trials": 1,
  "approval_mode": "approve_all",
  "expectations": {
    "outcome": {
      "must_contain": [
        { "path": "service.py", "text": "return \"ready\"" }
      ],
      "verification_command": "python service.py",
      "verification_must_contain": "ready"
    },
    "trajectory": {
      "must_use_tools": ["read_file", "edit_file"]
    },
    "recovery": {
      "require_recovery_hint": true,
      "require_retry_after_error": true
    },
    "judge": {
      "focus": ["detect ambiguous edit", "recover after exact-string edit failure", "finish with both returns set to ready"]
    }
  }
}
